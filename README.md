# Pre-requirements

In order to make the script work, run the following commands in the terminal :

`python3 -m pip install mediapipe==0.10.9`

`python3 -m pip install opencv-python mediapipe`

Also here I'm using python 3.11.17, avoid using recent versions in order to make mediapipe work without errors
